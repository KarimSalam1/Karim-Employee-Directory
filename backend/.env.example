# MongoDB connection string (Atlas or local)
MONGODB_URI=mongodb://localhost:27017/employee-directory

# Imgur API client ID, used for avatar uploads
IMGUR_CLIENT_ID=

# Comma-separated list of allowed CORS origins. Falls back to * when unset.
CORS_ORIGIN=

# Local dev server port (ignored on Vercel)
PORT=3000
